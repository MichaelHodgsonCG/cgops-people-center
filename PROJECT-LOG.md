# Project Log

## [2026-08-13] Gap seat owners + My Tasks (Menu Center's owner model)
**Shipped:** Per-SEAT ownership on staffing gaps, copied from Menu Center's launch-list owner model per Michael's ask. Migration 20260813120000 (APPLIED live): new table people_center_gap_assignments — one row per open seat at a (location × role-or-pool) cell with owner + support (person link + name snapshot, free text allowed), target date, and note; RLS = gap viewers read all, every user reads their own rows, admin/executive write; constraints sanity-tested live (unique seat, one-target check; test rows removed). Gap Analysis gains an Owner column (company + single-location tables) opening a per-seat editor with PersonPicker typeaheads (exact roster names auto-link, like Menu Center); the company Excel export gains an Owner column. New My Tasks nav view for ALL roles: seats assigned to you across every location, bucketed overdue / 7 days / 14 days / later by target date (falling back to the site's needed-by; understaffed with no date = ASAP); tasks self-clear when the seat is filled because gaps stay computed live. Deep link /?view=my-tasks ready for the CGOPS My Day tile (Menu Center's pattern). Build passes.
**Roadmap:** Phase 3 gap analysis -> in progress (ownership + dates layer done; per-role hiring lead times still backlog).
**Decisions:** Seat-specific owner/support rather than one owner per location — Michael's example: the Chef hires the Sous, the ROL hires the GM with the VP supporting. Pool gaps key assignments on the pool NAME, not id (pool ids are recreated on override; names survive). No "complete" button — a task disappears only when the seat is genuinely filled (slated/assigned/hired). When a cell's gap shrinks, the first N assigned seats (by seat number) count as still open.
**Blockers:** Surfacing these tasks inside CGOPS My Day needs a small tile/link added in the CGOPS launcher — the People Center deep link is ready and matches how Menu Center's My Tasks is already linked.
**Next:** Michael assigns owners on real gaps; add the CGOPS My Day link; per-role hiring lead times (date default becomes manual target > role lead time > site handover).

## [2026-08-13] Session log + bus filing protocol adopted (v2, CG)
**Shipped:** Adopted the CG session-log + filing protocol (v2). Persisted it verbatim to a new CLAUDE.md at the repo root (including the bus ref: Supabase project qzzhifdwoixqjgugbevq / cgops-platform, table cc_project_artifacts) so every future session inherits it without a paste — persistence confirmed. Verified the bus is reachable and filed this entry to it. No application code changed.
**Roadmap:** Session log + bus filing protocol -> complete.
**Decisions:** Matched the bus conventions already used by sibling CG projects (Menu Center, CGOPS Platform): metadata.project="People Center", metadata.filed_by="People Center (Claude Code)", title mirrors the local log entry. Kept PROJECT-LOG.md's existing house format (## [date] + bold labels) rather than the protocol's plain-text shape — same fields, established style.
**Blockers:** none.
**Next:** Resume feature work; file a log entry to the bus at the end of every session per CLAUDE.md.

## [2026-07-24] Gap analysis: multi-location picker + role filter
**Shipped:** Two filters on the Gap Analysis view. (1) Multi-location picker — the single View dropdown became a checkbox popover: no selection = company-wide, one = the detailed single-location table (unchanged), two+ = the company report filtered to that subset. Because backfill/movers are still computed company-wide in fetchCompanyGaps, a subset view stays correct — only what's shown is filtered. (2) Role filter — a second popover to show only chosen roles (e.g. "Sous Chef" → how many needed across sites), applied to both the company table and the single-location table; summary chips + Excel export follow the active filters. Added location_id + position_id to CompanyGap for id-based filtering (was name-only). New reusable FilterMenu + CheckGroup components. Build passes.
**Roadmap:** Gap Analysis now slices by any set of locations and/or roles. Common ask ("how many Sous Chefs do we need") = pick the Sous role, leave locations on All → per-site rows + total.
**Decisions:** Empty selection = all (both filters). One location keeps the rich per-role table; 2+ uses the company Location/Role/Gap/Type table. Filters compose; export mirrors the on-screen filtered set.
**Blockers:** none.
**Next:** —

## [2026-07-24] Multi-seat follow-ups: Excel import + Bench grid
**Shipped:** Closed the two multi-seat gaps left by the previous entry. (1) Excel import (gaps/api.ts): fetchSlotIndex now returns a LIST of seats per (location, role) with each seat's incumbent; applyAssignments fills VACANT seats first and creates new ones as needed, never overwriting a seat already held by someone else — so importing 3 Sous for one site lands on 3 separate seats instead of clobbering one. Idempotent (a person already slated for the role is a no-op). (2) Bench coverage grid (BenchView): incomingByCell now collects ALL incumbent names per cell (de-duped) and the pipeline cell renders every "(incoming)" name, not just the last one. Also routed the import's error text through the shared errText. Build passes.
**Roadmap:** Multi-seat succession (3 Sous, etc.) is now consistent across the Bench list, gap counts, Bench grid, and the Excel round-trip.
**Decisions:** Import fills vacant-before-create and never overwrites an occupied seat (safe re-import); over-filling beyond the required count just creates extra seats (visible in the gap view), not an error.
**Blockers:** none.
**Next:** —

## [2026-07-24] Multiple seats per role per location + real Bench errors
**Shipped:** Two fixes from a Bench failure. (1) A location can now have MULTIPLE succession seats for the same role — e.g. Beertown Peterborough requires 3 Sous Chefs. Migration 20260724160000 drops the one-seat-per-role unique index (people_center_succession_slots_unique_seat); the required roster already carries per-role counts and gap views already accumulate multiple incumbents. Verified: a 2nd Sous seat at Peterborough now inserts (was a unique_violation). (2) Bench errors rendered as "[object Object]" — PostgREST errors are plain objects, so `String(err)` printed junk and hid the real cause (the unique violation above). New shared src/lib/errText.ts (promoted from DataSourcesView's local helper) reads .message/.details/.hint off the object; wired through BenchView's catches. Build passes.
**Roadmap:** Unblocks planning real rosters where a role repeats (Sous ×3, etc.). The incoming-hire union (prior entry) already let assignments show; this lets the Bench/succession path plan multiples too.
**Decisions:** Removed the hard uniqueness rather than making it per-role-configurable — the required roster is the source of counts; the Bench should freely plan seats. Minor known degradations left as follow-ups: the Excel import's fetchSlotIndex and the Bench coverage-grid "(incoming)" cell are keyed one-per-(location,role) so they last-write-wins with multiples; the Bench slot list + gap counts are correct.
**Blockers:** none.
**Next:** (follow-up) make the Excel import + coverage grid multi-seat aware.

## [2026-07-24] Upcoming rosters now count incoming external hires (not just slated)
**Shipped:** An external hire added via the "add incoming hire" flow (person status='incoming' + a PRIMARY position assignment at an opening site) now appears on that site's roster. Bug: opening-site rosters read ONLY succession (slated) leaders and explicitly skipped position assignments, so Gary Madine — external Beverage Manager assigned to Beertown Peterborough — was invisible and Peterborough still showed a Beverage Manager gap. Fix (frontend only, no schema): opening-site fill = slated incumbents ∪ incoming/active/leave PRIMARY assignments at the site, de-duped by person. Touches gaps/api.ts (fetchFillForLocation upcoming branch + fetchCompanyGaps opening branch — projected/detail now union both; slatedByCell carries person ids for dedup), upcoming/api.ts (new fetchIncomingSeats), UpcomingView (merge slated + incoming per location|position, a succession seat wins unless unnamed and an incoming hire fills it) → flows into the card list + PlannedOrgPanel. External hires get no false "moving from" flag (their only seat is the opening site). Open sites unchanged (still current active/leave). Verified: Gary (incoming) fills Peterborough Beverage Manager (required 1) → seat now filled everywhere. Build passes.
**Roadmap:** Makes the incoming-hire path work for upcoming sites (Michael's choice over slating each external hire via the Bench). Both placement paths (slate via Bench / assign via incoming-hire) now show on upcoming rosters.
**Decisions:** Count incoming on OPENING sites only (their whole roster is a future fill); open sites keep counting current active/leave (an incoming hire not yet started shouldn't mask an open seat). De-dup by person so slated+assigned counts once.
**Blockers:** none.
**Next:** —

## [2026-07-24] Covered-locations admin picker (draft PR — held for Michael's merge)
**Shipped (branch/draft PR, NOT merged to main):** A new "Covered locations" admin surface replacing the raw-SQL workflow for people_center_user_scopes (bespoke coverage grants). Pick a user → see their EFFECTIVE coverage with the derivation of every location visible (region-derived default "leads <region>" ∪ scope grants "Grant: region/location"), a can_view_all badge + banner (admin or the flag → sees everything, and a warning that scoping is bypassed), and add/remove grants (whole-region or single-location). New src/features/coverage/{CoverageView,api}.tsx; nav item + route + guard; new can() resource 'user_scopes' (admin/executive); UserProfile gains can_view_all. Migration 20260724150000 (APPLIED live to the shared project): widened people_center_user_scopes RLS from admin-only to admin+executive (SELECT all / INSERT / UPDATE / DELETE), self-SELECT preserved. Verified as-role: executive can read all 20 grants + insert; regional_leader insert = RLS violation. Build passes.
**Roadmap:** Fulfils the ratified consolidation groundwork (People Center = org-structure source of truth). Coverage model mirrors the RBAC build's people_center_my_coverage (region default ∪ scopes) + people_center_can_view_all.
**Decisions:** Widened scope management to admin+executive per the "admin/executive-only" requirement (RBAC build had it admin-only) — flagged for Michael's sign-off on merge. Effective coverage computed client-side from readable tables (regions/locations) + scopes (now exec-readable), backed by RLS. The picker shows upcoming sites too (unlike my_coverage, which joins CGOPS names and drops cgops_location_id-null rows).
**Blockers:** Held for Michael's merge (draft PR) — the UI stays out of production (Vercel deploys on main) until then; the migration is already applied (harmless/unused without the UI). NOTE surfaced during the build: several profiles carry can_view_all=true who arguably shouldn't (regional_leader Camilla + viewers dbeach/rsmith, the latter also holding scope grants that are moot while view-all is on) — looks like interim state from the concurrent RBAC/CGOPS chat; NOT changed here, but the picker makes it visible.
**Next:** Michael reviews/merges the draft PR. Onboarding of the 5 remaining ROLs + Jody is BLOCKED — none have a login in this project yet (no auth.users / CGOPS bridge row), so there is no auth_user_id to attach a profile to; they must each sign in once (like Camilla) before roles can be set.

## [2026-07-24] BACKLOG: admin "view as role" (preview what other roles see)
**Requested:** Michael wants a way, as an admin, to see exactly what another role sees — to test and have confidence that lower roles (regional_leader/location_leader/viewer/executive) are NOT exposed to sensitive info (notes, restricted notes, bench, out-of-region data).
**Why:** Trust-but-verify on the permission model as new roles (ROLs) come online; a self-serve check instead of asking an engineer to simulate a session.
**Design caution (important):** A UI-ONLY role toggle (flip can()'s role client-side and re-render) is NOT sufficient and would give FALSE confidence — RLS still returns admin-level rows to the admin's own JWT, so DB-gated content (notes via chain-visibility, succession, covered locations) would still be fetched; only buttons would hide. A faithful preview must exercise RLS AS the target identity. Options: (a) an impersonation token / edge function that runs reads as the chosen user (true fidelity, more infra); (b) a "what would X see" report that re-answers the visibility predicates server-side via the existing security-definer helpers (people_center_current_role scoped, people_center_is_above, people_center_covers_location, the notes SELECT policy) for a chosen role+person and shows the result set — lighter, read-only, no session swap. Recommend (b) as a first cut: an admin-only panel that picks a role (and person for chain/region scoping) and renders the visible directory/notes/bench exactly as RLS would grant them.
**Status:** Not started — captured for planning.

## [2026-07-24] Region ownership fix (stopgap) — align People Center with CGOPS
**Shipped:**   Corrected a Camilla/Caitlin region mix-up. Michael fixed it in CGOPS (moved the upcoming sites between clusters: Richmond Hill → Caitlin, Peterborough → Camilla) but the change did NOT propagate to People Center — there is no live region sync, and covers_location reads People-Center-local data (locations.region_id + the Phase-1-seeded regions.leader_person_id). People Center still had Camilla → Richmond Hill. Live data fix (execute_sql, no migration): moved Beertown Peterborough into Camilla's region and Beertown Richmond Hill into Caitlin's, and renamed the two regions to match CGOPS (BTN/BTWh/BTP = Camilla, BTBa/BTT/BTR = Caitlin). Leaders and open-site memberships unchanged. Verified as Camilla's real session: covers Peterborough = true, covers Richmond Hill = false.
**Roadmap:**   Stopgap only. Michael flagged (correctly) that hand-seeded region ownership in People Center is fragile — it shadows what CGOPS already owns and drifts on any CGOPS change.
**Decisions:** Fixed by moving the two LOCATIONS between regions (mirroring CGOPS), NOT by swapping leaders — swapping leaders would have wrongly moved the open sites (Barrie/Toronto, Newmarket/Whitby) too. Durable fix routed to the CGOPS chat: CGOPS should own region definition + membership + ownership, and People Center should SYNC it (same pattern as the Positions sync) so covers_location never needs hand-seeding.
**Blockers:**  none. Until the sync exists, any future CGOPS region change must be mirrored here by hand.
**Next:**      (CGOPS workstream) region sync into People Center. On this side: onboard Caitlin as ROL when she's logged in (she now correctly owns Richmond Hill).

## [2026-07-24] Notes: creation is now chain-scoped (no notes on peers/superiors)
**Shipped:**   Closed a note-CREATION hole. Previously any regional_leader/location_leader could write a leadership/development note on ANY person — including peers and people above them (note VIEWING was already chain-gated via people_center_is_above, but INSERT was not). Migration 20260724140000 (applied live): the notes INSERT with_check now allows manager roles (regional_leader/location_leader) to create a note only on someone STRICTLY BELOW them in the reporting chain (people_center_is_above(current_person_id, person_id)); admin/executive stay unrestricted (HQ talent capture is the feature's purpose, ADR 0008); the voluntary self fun-fact path is unchanged. UI (PersonPanel): the note composer is now gated per-opened-person — HQ altitude always; manager roles only when fetchViewerIsAbove (new RPC wrapper on people_center_is_above, executable by authenticated) is true — so no composer appears where a create would be rejected. Prompted by Michael seeing Camilla (ROL) able to open notes on peers/superiors. Verified live as Camilla (rolled back): note on a subordinate (Matthew Legault) = allowed; note on her manager (John Mackay) = RLS violation. Build passes.
**Roadmap:**   Security fix on the notes model — orthogonal to the ROL access phases. Company-wide roster VIEW for ROLs is intended (audits + cross-region moves); notes stay strictly chain-scoped for manager roles.
**Decisions:** Interpreted "no user should open notes on peers or levels above" as applying to MANAGER roles (regional_leader/location_leader) — admin/executive remain the HQ/talent altitude that captures notes company-wide by design (restricting them would gut the feature). VIEW was already correct (is_above in the SELECT policy); only INSERT + the UI composer needed tightening. Existing notes a manager already authored remain visible to them as author; going forward none can be created on peers/superiors.
**Blockers:**  none. If Michael wants executives chain-restricted too, that's a follow-on (would change the notes feature's altitude).
**Next:**      Onboard Caitlin as the other ROL when she's logged in; optionally add the missing signup trigger so new logins auto-appear in Users.

## [2026-07-24] Scoped regional-leader access — Phase 3 (region-scoped writes)
**Shipped:**   Regional Ops Leaders can now EDIT succession seats for their own region only — this is where covers_location (Phase 1) does work. Migration 20260724130000 (applied live): additive regional_leader INSERT/UPDATE/DELETE policies on people_center_succession_slots (gated by location_id + covers_location) and people_center_succession_candidates (scoped through their parent slot's location via EXISTS). Existing admin/executive write policies untouched (Postgres OR-combines permissive policies). Frontend BenchView: editing is now per-seat — executives/admins edit anything; a ROL sees edit controls (change incumbent, add/remove successor, delete, and "Plan a seat") ONLY for seats/locations in the region(s) they lead; the Plan-a-seat location dropdown is filtered to their covered sites. New fetchCoveredLocationIds(personId) mirrors covers_location for the UI. Least privilege: deliberately did NOT grant people INSERT/UPDATE or position_assignments writes to ROLs — Bench pickers reference existing people only, so person creation + staff reassignment stay executive/admin (matches "Plan only" scope). Verified live by simulating Camilla's session (rolled back): UPDATE her covered Richmond Hill slot = 1 row, UPDATE Caitlin's Peterborough slot = 0 rows, INSERT candidate in her covered slot = ok, INSERT candidate in Peterborough = RLS violation. Build passes.
**Roadmap:**   Scoped regional-leader access: Phases 1–3 done (live). Camilla's "build up the new locations" path is now real — she can slate leaders into her covered upcoming sites (e.g. Beertown Richmond Hill) in the Bench. Next: Phase 4 — onboard Camilla/Caitlin (each signs in once, then admin sets role=regional_leader in Users) and confirm in the live UI.
**Decisions:** Writes region-scoped via covers_location; reads stayed company-wide (Phase 2). UI edit-gating computed client-side from region→leader (fetchCoveredLocationIds) but the DB RLS is the real enforcement (verified both directions). Excel round-trip / Required roster in Gap Analysis stay admin/executive for now (they'd need a scoped people-candidate INSERT); the Bench per-seat path covers the "build up new locations" ask without that extra write surface.
**Blockers:**  none. Prereq for go-live unchanged: Camilla/Caitlin sign in once, then an admin sets role=regional_leader. Until then no one holds the role, so this is invisible in production (verified via simulation).
**Next:**      Phase 4 — onboard the two ROLs and verify in the live UI. Then optionally open the scoped Excel round-trip (adds a narrow status='candidate' people INSERT gated to covered locations).

## [2026-07-24] Scoped regional-leader access — Phase 2 (company-wide reads, read-only)
**Shipped:**   Regional Ops Leaders can now VIEW the whole company's bench. Migration 20260724120000 (applied live): widened SELECT on people_center_succession_slots + people_center_succession_candidates to include regional_leader (unconditional company-wide read, same as executive/admin) — NO covers_location scoping on reads. Rationale (Michael): succession plans move managers in/out of regions, and ROLs audit restaurants outside their own region, so they need full-company leadership context. Frontend: can('view','bench') now true for regional_leader → Gap Analysis + Bench & Risk nav appear with full company-wide data; BenchView is READ-ONLY for ROLs (new canEdit gate hides Plan-a-seat, change-incumbent, add/remove successor, delete — executives/admins keep full edit). GapView's editors already gate on admin/executive, so they're read-only for ROLs unchanged. Writes stay executive/admin (INSERT/UPDATE/DELETE untouched). Verified live by simulating a real regional_leader session (synthetic auth uid, rolled back): current_role=regional_leader, sees all 8 slots + all candidates, slot UPDATE affects 0 rows, candidate INSERT raises an RLS violation. Build passes.
**Roadmap:**   Scoped regional-leader access: Phase 1 (region ownership + covers_location) done; Phase 2 (company-wide reads, read-only) done (live). Next: Phase 3 scoped WRITES (succession seat create/edit/delete gated by covers_location so a ROL edits only their region; narrow people INSERT for status='candidate' for the Excel add-candidate path; people UPDATE + assignment writes stay executive/admin), Phase 4 verify + onboard Camilla/Caitlin.
**Decisions:** Reads company-wide, NOT region-scoped (Michael's call — cross-region succession + audits). This is also more correct: the company-wide gap/backfill computation needs all movers visible, which region-scoped reads would have broken. covers_location is reserved for Phase 3 writes only. Bench editing gated by a UI canEdit (admin/executive) now; ROL region-scoped editing lands in Phase 3, gated per-seat by covers_location (not via can()). Development-conversation notes stay chain-visibility gated (ADR 0008) — the Bench "stale conversations" KPI just reflects what the ROL can already see.
**Blockers:**  none. Prereq for go-live: Camilla/Caitlin must sign in once (creates their login) before an admin sets role=regional_leader in the Users panel. Until then no one has the role, so Phase 2 is invisible in production (verified only via simulation).
**Next:**      Phase 3 (scoped writes), then check in before it goes live per the agreed cadence.

## [2026-07-17] Scoped regional-leader access — Phase 1 (region ownership + scope helper)
**Shipped:**   Foundation for giving Regional Ops Leaders scoped access to plan their own restaurants (incl. upcoming). Migration 20260717090000 (applied live): added people_center_regions.leader_person_id, seeded from who each region's open-restaurant GMs report to (1:1). Added the single scope helper people_center_covers_location(loc) — admin/executive cover all; a regional_leader covers only locations in the region(s) they lead (existing + upcoming, via region_id); others none. NO policy uses it yet, so access behavior is unchanged. Key finding: Michael had already assigned upcoming sites to ROLs via region_id (regions named by cluster, e.g. BTN/BTWh/BTR = Camilla → covers new Richmond Hill; BTBa/BTT/BTP = Caitlin → covers new Peterborough). 3 "Pending (2027)" sites have no ROL yet. Verified seed: all 6 real regions have their ROL, function compiles.
**Roadmap:**   Scoped regional-leader access: Phase 1 done. Next (checked in with Michael first): Phase 2 scoped reads (Bench/Gap filter to covered locations; can() view for regional_leader), Phase 3 scoped writes (succession/assignments/people edits gated by covers_location), Phase 4 verify + onboard Camilla/Caitlin.
**Decisions:** Scope by REGION (not per-person chain or manual location list) — regions already cluster an ROL's restaurants AND the upcoming sites are pre-tagged, so it handles new locations for free. Made region→leader first-class (leader_person_id) rather than deriving in every policy. covers_location is the one reusable check.
**Blockers:**  none. Prereq for go-live: Camilla/Caitlin must sign in once (creates their login) before an admin sets role=regional_leader in the Users panel.
**Next:**      Phase 2 (scoped reads) after Michael's go-ahead.

## [2026-07-17] Gap analysis: Excel round-trip import (fill → upload → slate)
**Shipped:**   Round-trip on the gap report. The company export now has a blank "Assign to" column; fill in who should staff each open role, then "Upload Excel" (admin/executive) → preview → Apply. Resolution (gaps/importXlsx.ts, pure): Location/Role/person matched by name — an existing person LINKS, an unknown name is added as a NEW CANDIDATE (Michael's choice 2); ambiguous/unknown location/role → skipped with a reason. Apply (applyAssignments in gaps/api.ts): create candidate if needed, then set the location+role succession seat's incumbent (setSlotIncumbent if the seat exists, else createSlot — respects the one-seat-per-role unique index; extra comma-separated names ignored with a note). ImportPanel.tsx = upload → preview table (Link/New candidate/Skip) → Apply, nothing writes until Apply. Records slated leaders = the Bench/succession model (Michael's choice 1). Verified: xlsx write→read round-trip + resolution (link/create/error/extras/blank all correct); build passes; xlsx stays a lazy chunk.
**Roadmap:**   Gap report is now a full round-trip (export → plan in Excel → import as slated leaders). Closes the Excel export+import ask.
**Decisions:** Slated leaders via succession seats (choice 1); unknown names → new candidates (choice 2). Import only reads the COMPANY export (needs Location+Role columns). One seat per role (unique_seat index) → first name is the incumbent, extras noted. Preview-then-apply; RLS admin/executive on people + succession writes.
**Blockers:**  none (couldn't click the SSO'd UI; verified parse/resolve in Node + all writes reuse existing tested Bench/people APIs).
**Next:**      Per-concept required counts; opening-date urgency in the gap view; per-role hiring lead time.

## [2026-07-16] Gap export switched from Word to Excel (.xlsx)
**Shipped:**   The gap report "Download" now produces **Excel (.xlsx)** instead of Word. Uses the `xlsx` (SheetJS) lib already in the repo (import pipeline), dynamic-imported. Both company-wide and single-location exports, with column widths, header/total rows, and the summary line. Removed the `docx` dependency + gaps/docx.ts. Verified in Node: valid .xlsx (PK zip, 17 KB) and a round-trip parse (XLSX.read → sheet_to_json returns the rows) — which also confirms the parse-back path for the requested Excel-import feature.
**Roadmap:**   Export format -> Excel (Michael's request). Requested next: accept a filled-in Excel back and parse it (scoping — what the upload should do, e.g. record who's slated into each open role).
**Decisions:** Reuse xlsx (already a dependency) over adding a spreadsheet lib; drop docx entirely (no dual formats). writeFile in-browser triggers the download.
**Blockers:**  none
**Next:**      Scope + build the Excel round-trip import (fill gaps → upload → apply). Then per-concept required counts / urgency.

## [2026-07-16] Gap analysis: sortable company-wide table
**Shipped:**   The company-wide gap table now has sortable column headers — click Location, Role (by seniority/level), Gap (biggest first), or Type (new-site → backfill → understaffed) to re-sort; arrow indicator on the active column. Client-side sort of the already-fetched rows (mirrors the Directory's SortableHeader pattern). The .docx export follows the on-screen sort. Build passes.
**Roadmap:**   Gap report usability tweak. Company-wide report + backfill remain complete.
**Decisions:** Role sorts by position level (groups same roles, GM→Sous) not alphabetical; Gap defaults to descending (biggest gaps first); default view stays grouped by Type.
**Blockers:**  none
**Next:**      Per-concept required counts; opening-date urgency in the gap view; per-role hiring lead time.

## [2026-07-16] Gap analysis: company-wide report + backfill from moves
**Shipped:**   Gap Analysis now defaults to a company-wide report — every missing management role across all locations in one view — classified new-site / backfill / understaffed. Backfill is the new piece Michael asked for: an existing leader slated to a new site vacates their seat, so the origin needs a replacement; the report names the move (e.g. Whitby GM ← Matthew Legault → Peterborough; Burlington Chef ← Matt Carroll + Harry Patel leaving). fetchCompanyGaps() computes projected = current − movers per (location, role); summary chips (New-site 34 / Backfill 3 / Understaffed 35 / Total 72). Single-location mode retained via the picker. .docx export now covers whichever view is showing (added downloadCompanyGapDocx). Consistency fix: both views count PRIMARY assignments so they never disagree (verified Waterloo identical under primary vs all-open). Verified: backfill math (3 backfills — Burlington/Guelph/Whitby), full totals, real-CSS render. Build passes.
**Roadmap:**   Company-wide gap report + backfill -> done (live). This was the missing piece — the earlier export was per-location and ignored backfill.
**Decisions:** A mover = someone in-seat at an open site who is a succession incumbent at an opening site; they're subtracted from the origin's projected headcount. Count primary assignments (one seat per person) in BOTH modes for consistency. Three reason buckets over a flat list so hiring can be prioritized.
**Blockers:**  none. Observation: ~35 understaffed gaps at existing sites — many don't match the ideal roster (e.g. no Guest Service/Beverage Mgr). Real; Michael can trim required counts or set per-concept later.
**Next:**      Per-concept required counts; opening-date urgency in the gap view; per-role hiring lead time.

## [2026-07-16] Phase 3: gap report Word (.docx) export
**Shipped:**   "Download .docx" button on the Gap Analysis view generates a Word report for the selected location — heading, "Upcoming (slated)"/"Open (in seat)" subtitle, generated-date, summary line, the role table (Role / Required / In-seat|Slated / Gap / People), and a totals row. Added the `docx` library, dynamic-imported inside gaps/docx.ts so it builds a separate lazy chunk (~355 kB, only loads on export — main bundle stays lean). Verified in Node: the exact Document/Table construction packs to a valid .docx (PK zip signature, 8.9 KB). Build passes.
**Roadmap:**   Phase 3 deliverable (in-app gap report + downloadable Word) -> complete (live). Remaining refinements: per-concept required-count overrides, opening-date urgency in the gap view, per-role hiring lead time.
**Decisions:** Dynamic-import docx (lazy chunk) rather than bundling it into the main app. Report mirrors the on-screen table exactly (management roster only). Filename "<Location> - Gap Analysis.docx".
**Blockers:**  none
**Next:**      Per-concept required counts / date-driven urgency in the gap view, or the per-role hiring-lead-time backlog item.

## [2026-07-16] Phase 3 v1: gap analysis (required roster vs in-seat/slated)
**Shipped:**   New "Gap Analysis" nav view (gated by bench permission). Pick a location → required management roster vs. who's in seat (open sites: active/leave people in position assignments) or slated (opening sites: succession incumbents); shows shortfall per role + totals. Required counts in new table people_center_role_requirements (migration 20260716130000 applied live; one base template; admin/executive edit inline via "Required roster" panel). Confirmed base roster (Michael, "1"): GM/Chef de Cuisine/AGM/Service/Beverage/Guest Service = 1, Sous Chef = 2 (Events + GM-in-Training = 0). Scope = management only. gaps/GapView.tsx + gaps/api.ts; AppShell nav + App route + guard. Verified: gap math (Peterborough opening = 6 short of 8, GM+Chef slated; Beertown Waterloo open = fully staffed) and real-CSS render. Build passes.
**Roadmap:**   Phase 3 v1 (config + in-app gap report) -> done (live). Remaining: Word (.docx) export; per-concept required-count overrides; opening-date urgency in the gap view.
**Decisions:** Interpreted Michael's "1" as option 1 across the board — one base template (not per-concept yet), management-only scope, proposed counts as-is; all editable in-app so easy to change. Existing sites count active/leave in-seat; upcoming sites count slated (succession). Required counts readable by all authenticated, writable admin/executive.
**Blockers:**  none
**Next:**      Word (.docx) export of the gap report (add a docx lib). Then per-concept overrides / date-driven urgency, and the per-role hiring-lead-time backlog item.

## [2026-07-16] Planned org shows full management roster (not just slated seats)
**Shipped:**   The per-site Planned org now renders the whole restaurant management roster, not only the roles with succession slots. Seeds the tree from positions where default_person_kind='manager' AND people_center_eligible=true (GM, GM-in-Training, Chef de Cuisine, AGM, Service/Events/Guest Service/Beverage Manager, Sous Chef), shaped by the reporting template; fills from succession, everything else OPEN. Corporate roles (eligible=false: CEO/Directors/Regional Ops) and line/emerging roles (Supervisor, Chef de Partie) are excluded. fetchPositionTemplate now returns default_person_kind + people_center_eligible. Verified render (Peterborough: GM + Chef slated, 7 OPEN seats incl. AGM, the 4 level-30 mgrs, Sous, GM-in-Training). Build passes.
**Roadmap:**   Planned-org roster complete. Note: "GM in Training" shows as an OPEN seat because it's manager+eligible — Phase 3's per-concept required roster will let Michael trim roles like that from the base template.
**Decisions:** Data-driven roster (manager + eligible) rather than a hardcoded role list — no name-based special cases; Phase 3 makes the exact required roster (and counts) configurable per concept.
**Blockers:**  none
**Next:**      Phase 3 — gap analysis (current vs required per location) + Word (.docx) export. Needs Michael's required-count-per-role input (base template + per-concept carve-outs).

## [2026-07-16] Phase 2 slice 3: per-site planned org + move flags
**Shipped:**   "Planned org" modal on each Upcoming card (admin/executive) — an upcoming restaurant's planned leadership as the standard hierarchy: seats from the position template (default_reports_to), each filled with the slated leader (succession incumbent) or an OPEN gap. Unslated template ancestors are synthesized (a slated Chef with no GM shows an OPEN GM above it). A slated leader who currently holds a seat elsewhere gets a "moving from <site>" flag — surfacing the knock-on vacancy. Read-only; edits stay in the Bench. New PlannedOrgPanel.tsx; api fetchPositionTemplate + fetchCurrentPrimaries. No new data model — reuses succession + template + current assignments. Verified: real-CSS render (Peterborough GM Matthew Legault + Chef Randy Littlejohn both "moving from Beertown Whitby"; Richmond Hill synthesizes OPEN GM above slated Chef Harry Patel) and the move data (every slated leader is an internal move). Build passes.
**Roadmap:**   Phase 2 slice 3 (per-site planned org) -> done (live). Chosen over a full company-wide ghost overlay (deferred — needs region/parent inference).
**Decisions:** Tree shape from the position template; OPEN gaps by synthesizing unslated ancestors up to the root; "moving from" computed from each slated person's current open primary assignment. Per-site (modal) rather than splicing ghost nodes into the live company chart — self-contained, no clutter, no parent inference.
**Blockers:**  none. Knock-on insight is real: Peterborough takes both GM + Chef from Beertown Whitby, so Whitby needs both re-hired.
**Next:**      Backlog: per-role hiring lead time; optional company-wide ghost overlay. Or Phase 3 (gap-analysis Word report; needs required-count-per-role).

## [2026-07-16] Phase 2 slice 2 CORRECTED: reflect succession, drop redundant table
**Shipped:**   Reworked the Upcoming card's planned-leadership section to REFLECT the existing succession plan (read-only) instead of a separate store. Michael pointed out the Bench already plots leaders into the new restaurants. Root cause of my error: I concluded upcoming sites weren't in people_center_locations because their `cgops_location_id` is null — but they ARE there (status='opening') and already have succession slots. Dropped the redundant people_center_opening_placements table (migration 20260716123000; it was empty, no data lost) and removed its CRUD/edit UI. Upcoming now reads people_center_succession_slots (same embeds as the Bench), filtered to status='opening' locations, matched to opening_sites by name (no id link). Shows "role — slated person" ordered by seniority, or "not yet named"; admin/executive only. Verified: table gone, reflection query returns the real plan (Peterborough → GM Matthew Legault + Chef Randy Littlejohn, Richmond Hill → Chef Harry Patel, …) all name-matched to opening_sites. Build passes.
**Roadmap:**   Phase 2 slice 2 -> done, consolidated onto succession (one source of truth; edit in Bench). Slice 3 (org-chart overlay) can key off the status='opening' locations directly.
**Decisions:** One planning store — succession (Bench) — not two. Upcoming = New Restaurant Center dates + a read-only reflection of the Bench plan. Match opening_sites↔location by name since the upcoming location rows have cgops_location_id null. Superseded the same-day people_center_opening_placements design.
**Blockers:**  none. Lesson: check a location's existence by name/status, not just the cgops_location_id link (it's null on upcoming rows).
**Next:**      Backlog: per-role hiring lead time. Then Phase 2 slice 3 (org-chart overlay) or Phase 3.

## [2026-07-16] Phase 2 slice 2 (superseded): slated incumbents for upcoming sites
**Shipped:**   Each Upcoming card now has a "Planned leadership" section (admin/executive only): slate who fills a role at an upcoming site — role + person, or "TBD (gap)" — add/remove inline; list ordered by role seniority. New table `people_center_opening_placements` (opening_site_id + position_id + nullable person_id + note), migration 20260716120000 applied live, RLS admin/executive on all ops (mirrors succession tables). api.ts: fetch/add/remove with audit. Reuses fetchReferenceOptions (roles) + fetchManagerCandidates (people). Verified: migration (table + 4 policies), an insert→join→delete round-trip against the real FKs, build passes.
**Roadmap:**   Phase 2 slice 2 (slated incumbents) -> done (live). Slice 3 (org-chart overlay of a future site's planned team) next — still needs upcoming sites brought into people_center_locations (pc_location_id null today).
**Decisions:** Did NOT reuse people_center_succession_slots — its location_id hard-references people_center_locations, which upcoming sites lack; a dedicated table keyed to opening_site_id avoids materializing locations early and keeps future-planning data out of the live Bench/succession. person_id nullable = an explicit gap marker ("TBD"). Planning section gated to admin/executive (RLS + UI), matching succession sensitivity.
**Blockers:**  none. New backlog item (Michael): per-role hiring lead time (days-before-opening by role) to make the staffing countdown role-aware — captured in HANDOVER.
**Next:**      Backlog: per-role hiring lead time. Then Phase 2 slice 3 (org-chart overlay) or Phase 3 (gap-analysis Word report; needs required-count-per-role).

## [2026-07-16] Phase 2 slice 1: Upcoming locations view (NRC opening dates)
**Shipped:**   New "Upcoming" nav view (gated by org-chart permission) — a read-only projection of the New Restaurant Center's `opening_sites`. Lists planned restaurants with handover / soft-opening / opening dates, sorted soonest-first, each with a staffing-deadline countdown coloured by urgency (≤45d danger, ≤120d warning, else info; undated = muted). E.g. Beertown Peterborough "in 88 days", Richmond Hill "in 109 days". Reads `opening_sites` directly (its SELECT policy is `qual=true` for authenticated) — no sync, no new model. Verified against the real built CSS with the 5 live sites. AppShell nav + App routing wired; SuggestionsPanel context added.
**Roadmap:**   Phase 2 slice 1 (future locations + staffing deadlines) -> done (live). Next: slice 2 slated incumbents (reuse people_center_succession_slots/candidates — the Bench model), slice 3 org-chart overlay.
**Decisions:** Read `opening_sites` directly rather than syncing into people_center_locations — it's read-only display and the New Restaurant Center owns the data (ADR 0012 sync is for vocabulary People Center edits). Handover date = the headline staffing deadline. NRC tables mapped: opening_sites (+ opening_playbooks/tasks/templates).
**Blockers:**  none. Note: upcoming sites are NOT in people_center_locations yet (pc_location_id null) — the slice-3 org-chart overlay must bring them in or join by cgops_location_id.
**Next:**      Phase 2 slice 2 (slated incumbents per upcoming site) or Phase 3 (gap-analysis Word report; needs required-count-per-role).

## [2026-07-16] Cleaner mobile Directory (responsive card layout)
**Shipped:**   Directory is now mobile-first. On phones the horizontal-scroll table becomes a stacked, tappable card list (name, position · location, kind/status/HQ badges, needs-review icon); filters (search, position, location, kind) go full-width. Desktop keeps the table at the sm+ breakpoint. Bulk-edit selection works on cards too (checkbox + orange highlight); tapping a card opens the cheat-sheet panel. Verified against the real built CSS bundle at 390px width via a Chromium screenshot. Frontend-only.
**Roadmap:**   Backlog item "cleaner mobile Directory" -> done (live). Complements the mobile Visit view.
**Decisions:** Made the existing Directory responsive rather than adding a separate field view — avoids duplicating the people list + filters + bulk logic (one source of truth). Cards use a clickable div (not a button) so the bulk checkbox isn't a nested interactive element.
**Blockers:**  none
**Next:**      Phase 2 scoping (upcoming-locations view + New Restaurant Center opening/turnover dates) or Phase 3 (gap-analysis Word report; needs required-count-per-role input).

## [2026-07-16] Directory bulk-edit status (prune roles) + mobile-directory backlog
**Shipped:**   Directory now has an admin "Bulk edit" mode: filter by position/role → toggle Bulk edit → tick rows (or "select all filtered") → pick a status → Apply. Statuses offered: Departed (removes from directory + org chart — the prune mechanism), Leave, Active. Confirm dialog; fail-loud batch write (api bulkSetStatus, RLS admin/executive) with one audit row per batch; reversible (set Active again). This is the tool for pruning non-pipeline Supervisors (roadmap 3). Verified against the Push sync: roster-linked people are recognized as known duplicates and "not modified" on re-sync, and departed people are excluded from the manual-match pool — so a departed supervisor stays departed. Build passes.
**Roadmap:**   Item 3 (prune non-emerging-leader Supervisors) -> tooling shipped (live); Michael applies the pruning in-UI. Added to backlog: cleaner mobile Directory page with better in-field filtering (complements the Visit view).
**Decisions:** Reuse the Directory's existing position filter as the "per role" selector rather than a bespoke per-role UI; status-based pruning per Michael's ask (departed = the hide). One batch audit row over N per-person rows. Curated status set (departed/leave/active); onboarding states (candidate/incoming) not bulk-applied.
**Blockers:**  none. Caveat for the record: an UNLINKED departed person (no roster source_key) could be re-imported as a new active row by a future sync — does not affect roster-sourced supervisors (they're linked).
**Next:**      Cleaner mobile Directory (new backlog item), or Phase 2 scoping (New Restaurant Center dates).

## [2026-07-16] Org chart: rank-staggered sibling vines (chart view)
**Shipped:**   Chart (flow) view now staggers siblings vertically by rank so seniority reads visually: a Chef de Cuisine (level 20) sits above the Beverage/Service Managers (30) that share its GM, instead of all direct reports sitting on one flat row looking like equals. Siblings group into tiers by position level (most-senior tier = 0); each lower tier drops a fixed 26px and its connector "vine" lengthens to reach it. Same-level siblings stay aligned; reporting data (manager_person_id) is untouched — pure layout. CSS driven by a per-node --oc-drop var; drop computed in ChartNode via siblingDrop(). Verified with a standalone Chromium screenshot of the exact CSS. List view unchanged (already indents).
**Roadmap:**   Addresses Michael's "managers look equal to the chef" feedback — the visual-rank ask, distinct from reporting-line edits. Live on main.
**Decisions:** Stagger by distinct-level tier (bounded, same-level rows align) rather than by absolute level delta; 26px step. Chart-view only (scoped to .oc-tree). Kept the shared top rail straight and only lengthened each child's vertical drop — matches the "extend the downward vines" request.
**Blockers:**  none
**Next:**      Roadmap 3 (prune non-emerging-leader Supervisors) or Phase 2 scoping. Michael still working supervisor/reporting-line adjustments in-UI.

## [2026-07-16] Session wrap — all work deployed live; paused
**Shipped:**   Confirmed Vercel auto-deploys on every `main` push (handover corrected — the old "manual deploy" note was stale). All of this session's work is live at cg-people-center.vercel.app: org-chart seniority ordering, GMs→Regional Ops verified + Caitlin dedup, and the admin reporting-line (manager) editor. Hard refresh clears stale cache (the reason ordering looked unapplied mid-session).
**Roadmap:**   Items 1 + 2 -> complete (live). Reporting-structure editor -> shipped. Remaining: item 3 (prune non-emerging-leader Supervisors), Phase 2 (future org view + New Restaurant Center dates — its Vercel project is `cg-new-restaurant-center`, same DB), Phase 3 (gap-analysis Word report; needs required-count-per-role).
**Decisions:** Supervisor pruning, CdP→Sous assignment, and the flat-vs-nested FOH question are leadership calls — Michael will work them in-UI via the new reporting-line editor rather than SQL.
**Blockers:**  none
**Next:**      Resume tomorrow. Michael adjusts reporting lines/supervisors in the UI; then pick item 3 (quick win) vs. scoping Phase 2.

## [2026-07-16] Admin reporting-line editor (set manager in-UI)
**Shipped:**   Admins can now set a person's reporting line (manager) from the person panel's admin editor — a "Reporting line (manager)" picker listing active org members (labeled Name — Position · Location), writing manager_person_id. The person's own descendants + self are pruned from the list so a reporting loop can't be created. Fail-loud on RLS (same guard as other people writes) + records an audit row and a reporting_line.changed timeline event. Lets leadership rewire the org chart (e.g. Chef de Partie → the right Sous Chef) without SQL; chart redraws live. api.ts: fetchManagerCandidates() + setManager(). Frontend-only — deploy on Vercel to use.
**Roadmap:**   Reporting-structure admin settings -> shipped (code). Enables self-serve fixes for the CdP→Sous nesting (which Sous is leadership's call — no station data exists to infer it).
**Decisions:** Per-person manager picker in the existing AdminEditor (lowest friction, matches position/location reassignment pattern, chart redraws live) over a separate admin page; native select with descendant-pruning for cycle safety over a heavier typeahead (can upgrade later). Reporting-line change gets its own timeline event since it's business-meaningful. Confirmed RLS: people_center_people UPDATE allows admin/executive, no blocking triggers.
**Blockers:**  none
**Next:**      Deploy (batches with the undeployed item-1 seniority ordering). Then leadership sets CdP→Sous lines themselves; revisit whether a bulk reassignment tool is worth it for the ~39 Chef de Parties.

## [2026-07-16] Restaurant GMs joined to Regional Ops (roadmap 2) + Caitlin dedup
**Shipped:**   Verified the company chart is one connected tree: all 16 restaurant GMs → their Regional Operations Leader → John Mackay → Jody (CEO). The GM→ROL links were already set by the 1c HQ seed, so item 2 was functionally done. Found + fixed the one blemish: a duplicated Regional Operations Leader (HQ-seed off-roster stub with the title but no team vs. the real roster record with the 2 GMs but no title). Consolidated onto the roster record — added the ROL title to it (GMs already attached), ended the stub's assignment and marked the stub departed so it drops off the chart. Live data op (no code change). Final: Tami 4 GMs, Chris 3, Caitlin/Camilla/Danny 2 each (all → John Mackay); Cindy 3 (→ CEO).
**Roadmap:**   Roadmap item 2 (GMs → Regional Ops Leader, one company-wide chart) -> complete (live). No migration — data-only.
**Decisions:** Consolidate the duplicate onto the real roster record, not the seed stub (keeps the true identity + correct "Caitlin" spelling); retire the stub via status=departed (reversible) rather than hard delete. The stub/roster pair slipped past exact-name dedup because the seed misspelled the surname vs. the roster.
**Blockers:**  none
**Next:**      Roadmap 3: a way to ignore/remove non-emerging-leader Supervisors from the pipeline view (Michael's ask).

## [2026-07-16] Org-chart siblings ordered by seniority (roadmap 1)
**Shipped:**   OrgChartView now orders sibling nodes by position `level` (lower = more senior: GM 10 → AGM 25 → Sous 40 …) with full name as tiebreaker, instead of alphabetically. Query pulls `people_center_positions.level`; unranked positions sort last so a missing level never jumps someone above real managers. Applies to both list and chart views (they share `buildForest`). Frontend-only — deploy on Vercel to see it.
**Roadmap:**   Roadmap item 1 (org-chart seniority ordering) -> complete (code; deploy pending). Build passes (tsc + vite).
**Decisions:** Sort siblings only (children of each node); roots keep their existing descendant-count order. Unranked/null level -> Infinity (sorts last) rather than 0, so unconfigured positions don't masquerade as most-senior.
**Blockers:**  none
**Next:**      Roadmap 2: connect restaurant GMs to their Regional Ops Leader (set manager_person_id) for one company-wide chart.

## [2026-07-16] Michael admin fix + position ranking (1b) + HQ seed (1c)
**Shipped:**   (1) Root-caused the Positions-sync rejection: Michael's admin profile was linked to a deleted auth user, so the UI trusted him (role by email) but every DB write failed is_admin() (matches by auth.uid()). Re-pointed to his live auth user — all admin writes work now, no re-login. preferred_name "Michael"; login display untouched. (2) 1b: position levels (GM 10 -> Chef de Partie 50) + default_reports_to accountability lines + Senior Sous Chef. (3) 1c HQ seed: added 22 HQ titles to CGOPS positions (mirrored into People Center), created Head Office location, seeded 31 HQ people (20 new + 11 existing stubs reconciled) all off-roster with reporting lines + titles per the confirmed HQ chart. Verified tree matches.
**Roadmap:**   1b ranking + 1c HQ seed -> complete (live). Repo migration 20260716090000 (ranking) committed. HQ seed is a live data op (employee names kept out of the repo).
**Decisions:** HQ modeled as off-roster people at a "Head Office" location so titles render. HQ titles added CGOPS-side then mirrored. Riley/Darryl -> Todd Clarmo (single-manager approx of "report to Todd & Michael"). Cindy Fawcett -> CEO; Issac/Jennifer/Shanna -> CEO (change next fiscal). Chelsey -> Megan.
**Blockers:**  none. Supervisor left people_center_eligible=true (Michael's call).
**Next:**      Frontend: order org-chart siblings by position.level (data now exists). Connect restaurant GMs to their Regional Ops Leader. Queued: a way to ignore/remove supervisors who aren't emerging leaders. Then Phase 2 (upcoming-locations future view + New Restaurant Center dates) and Phase 3 (gap-analysis Word report; needs required-counts per role).

## [2026-07-15] Mobile "Visit" walk-in + org rules locked
**Shipped:**   New "Visit" nav view — mobile-first location → managers → notes: pick a restaurant, see its people seniority-ordered, tap to open the cheat sheet and read/add/save notes; last location remembered; big touch targets. Also fixed the Positions-sync error display ([object Object] → real Postgrest message). Both frontend-only; deploy on Vercel to use.
**Roadmap:**   1a Visit walk-in -> complete (code; deploy pending). Sync error-display fix -> shipped.
**Decisions:** Restaurant template locked — AGM sits between rank 2/3, reports to GM; FOH Supervisors -> GM by default; kitchen Chef de Cuisine > Senior Sous > Sous > Chef de Partie; Regional Ops Leaders -> John Mackay except Cindy Fawcett -> Jody (CEO); HQ titles created CGOPS-side then synced. Note: user is Chef Mike Hodgson — app session likely maps to a non-admin profile, the probable cause of the Positions-sync rejection.
**Blockers:**  Supabase paused at user's request — position-sync admin-role diagnosis, 1b (levels + template migration) and 1c (HQ seed) all wait on it.
**Next:**      When Supabase clears: confirm/fix sync admin-role; draft + apply 1b (position levels + per-concept required-role template) and 1c (HQ team seed from Chart 2).

## [2026-07-15] Position vocabulary sync (CGOPS -> People Center)
**Shipped:**   People Center positions now sync from the CGOPS Operational Center master instead of being a hand-seeded copy. New Data Sources → Positions tab: "Sync from CGOPS" button + a grid to toggle show-in-pickers / roster-eligibility / default kind per position. `people_center_sync_positions_from_cgops()` links by external_ref, refreshes CGOPS-owned fields, and materializes new CGOPS positions hidden+ineligible until curated. Pickers filter to curated positions; `Needs Position Review` placeholder hidden. Adding an HQ position = add once in CGOPS, sync, toggle on.
**Roadmap:**   Position vocabulary sharing -> complete (code shipped; migration 20260715120000 applied + verified on live project)
**Decisions:** Sync two tables over one shared table (ADR 0012) — the two lists are different curations (membership/granularity/columns), so sharing re-encodes the divergence + forces a live FK migration + couples PC to CGOPS lifecycle; field ownership (CGOPS=name/desc, PC=config) makes bi-directional editing safe; PC-side pull, not a CGOPS trigger, to keep PC logic off CGOPS's write path.
**Blockers:**  none
**Next:**      Deploy frontend; run first "Sync from CGOPS" (admin, in-app) to materialize the CGOPS catalog; toggle on HQ positions and fix Supervisor roster-eligibility drift in the same panel.

## [2026-07-15] Manual add + admin-confirmed Push linking
**Shipped:**   Add people by hand from the Directory — HQ (active, off-roster) and candidates (not-yet-hired), alongside incoming hires. A roster sync that name-matches an unlinked manual profile now holds it as a "possible match" (Data Sources → Pending links) for an admin to Confirm (links Push identity, preserves manual data) or Reject (imports as new). Candidates stay off the org chart. Confirmed answer to the sync-behavior question: uploads add + link, never merge/overwrite leadership-entered data.
**Roadmap:**   Manual people & Push linking -> complete (code); deploy + migration -> in progress (owner running)
**Decisions:** Admin-confirmed linking over auto-by-name (avoids false merges on common names); `candidate` status + `off_roster` flag over reusing active/incoming (distinguishes prospects and HQ; keeps sync from flagging HQ as missing); linking preserves manual data, only stamps `external_refs.push_source_key` + fills an empty assignment (upholds ADR 0005 "never overwrite"); reuse existing source_key correlation rather than a new link table.
**Blockers:**  Migration 20260709090000 must be applied to the CGOPS Platform Supabase project before the new UI works on live data (owner deploying).
**Next:**      Owner deploys + runs migration, then returns with feedback.
