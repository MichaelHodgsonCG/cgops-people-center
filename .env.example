# People Center — Supabase project `cgops-people`
# Copy to .env and fill in the anon key (Supabase dashboard → Settings → API).
# Server-side secrets (service role key) never go here — edge functions only.
VITE_SUPABASE_URL=https://jgwuaixztxatzjjxsvzc.supabase.co
VITE_SUPABASE_ANON_KEY=
